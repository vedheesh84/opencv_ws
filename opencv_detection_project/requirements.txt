# OpenCV Object Detection and Color Identification Requirements
# For Raspberry Pi 4 Model B with Ubuntu Desktop 22.04

# Core dependencies
opencv-python==4.8.1.78
opencv-contrib-python==4.8.1.78
numpy==1.24.3

# Raspberry Pi Camera support (picamera v1 for legacy camera)
picamera==1.13

# Optional: RPi.GPIO for hardware control (if needed)
# RPi.GPIO==0.7.1

# Note: Make sure to enable legacy camera support on Raspberry Pi
# Run: sudo raspi-config -> Interface Options -> Legacy Camera -> Enable

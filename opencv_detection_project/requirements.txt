# OpenCV Object Detection and Color Identification Requirements
# For Raspberry Pi 4 Model B with Ubuntu Desktop 22.04

# Core dependencies (optional - system packages recommended)
# opencv-python>=4.8.0
# numpy>=1.24.0

# IMPORTANT: On Ubuntu 22.04, install these via APT, not pip:
# sudo apt update
# sudo apt install -y python3-picamera2
# sudo apt install -y python3-libcamera python3-kms++
# sudo apt install -y python3-opencv python3-numpy

# Verify picamera2 installation:
# python3 -c "from picamera2 import Picamera2; print('Success!')"
